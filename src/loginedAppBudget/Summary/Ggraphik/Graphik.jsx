import React, { useEffect, useState, useRef } from "react";
import styles from "./Graphik.module.scss";

const Graphik = function ({ graphickData }) {
  const graphik = useRef(null);
  return <div></div>;
};

export default Graphik;
